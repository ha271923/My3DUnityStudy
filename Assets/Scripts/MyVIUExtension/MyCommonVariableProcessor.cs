using HTC.UnityPlugin.CommonEventVariable;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MyCommonVariableProcessor : MonoBehaviour
{
    [SerializeField]
    public CommonEventAsset inCommonEvent; // Drag to set .asset in UnityUI
    [SerializeField]
    public Text outText;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
